import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 10,
    borderRadius: 5,
    backgroundColor: '#000',
  },
  text: {
    color: '#fff',
    fontWeight: '600',
  },
});
